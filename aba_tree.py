from treelib import Node, Tree
class ABA_Tree(Tree):
    def admissible(self, nid):
        node = self.get_node(nid)
    
    def complete(self, nid):
        node = self.get_node(nid)
        
    def grounded(self, nid):
        node = self.get_node(nid)
    
    def conflict_free(self, nid):
        node = self.get_node(nid)
        
